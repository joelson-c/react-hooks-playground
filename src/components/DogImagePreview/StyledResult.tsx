import styled from 'styled-components';

export const StyledResultWrapper = styled.div`
  display: table; /* fit-content workaround */
  position: relative;

  margin: 0 auto;
`;

export const StyledResult = styled.img`
  max-width: 100%;
  height: auto;
`
